import { SxProps } from '@mui/material';

const boxStyleForgotPassword: SxProps = {
  marginTop: 8,
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
};

const avatarStyleForgotPassword: SxProps = {
  m: 1,
  bgcolor: 'secondary.main',
};

const resetPasswordTypographyStyle: SxProps = {
  m: 2,
};
const innerBoxStyleForgotPassword: SxProps = {
  mt: 1,
};

const resetPasswordButtonStyle: SxProps = {
  mt: 3,
  mb: 2,
};

export {
  boxStyleForgotPassword,
  avatarStyleForgotPassword,
  resetPasswordTypographyStyle,
  innerBoxStyleForgotPassword,
  resetPasswordButtonStyle,
};
